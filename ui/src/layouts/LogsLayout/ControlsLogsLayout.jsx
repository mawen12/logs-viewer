import classNames from "classnames"

const ControlsLogsLayout = ({headerSetup}) => {

    return (
        <div className={classNames({
            "lv-header-controls": true,
        })}>

        </div>
    )
}

export default ControlsLogsLayout;